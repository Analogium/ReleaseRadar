# URL de base de l'API backend.
# En dev, laisser vide : les requêtes /api/* passent par le proxy Vite (vite.config.ts).
# En prod, définir l'URL publique du backend, ex : https://api.exemple.com/api
VITE_API_URL=
